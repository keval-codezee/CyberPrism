<UserControl x:Class="CyberPrism.Modules.Dashboard.Views.MonthlySummary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    <Border Style="{StaticResource CardStyle}">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Monthly Summary" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
			<lvc:CartesianChart Grid.Row="1" Series="{Binding MonthlySummarySeries}" LegendLocation="Right" Margin="0,10,0,0" Foreground="White" DisableAnimations="True">
                <lvc:CartesianChart.AxisX>
					<lvc:Axis Title="Month" Labels="{Binding Labels}" />
				</lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis MinValue="0" MaxValue="120" />
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Grid>
    </Border>
</UserControl>

