<UserControl x:Class="CyberPrism.Modules.Analytics.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:core="clr-namespace:CyberPrism.Core.Views;assembly=CyberPrism.Core"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Advanced Analytics" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- OEE Trend -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <Grid Margin="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="OEE Trends (Last 7 Days)" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding OeeSeries}" LegendLocation="Bottom" Margin="0,15,0,0" Foreground="White">
                             <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="Mon,Tue,Wed,Thu,Fri,Sat,Sun"></lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>

                <!-- Downtime Analysis -->
                <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                    <Grid Margin="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Downtime Reasons" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding DowntimeSeries}" LegendLocation="None" Margin="0,15,0,0" Foreground="White">
                            <lvc:CartesianChart.AxisX>
                                 <lvc:Axis LabelFormatter="{Binding Formatter}" Foreground="White"></lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Labels="{Binding DowntimeLabels}" Foreground="Gray"></lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- Connection Error Overlay -->
        <core:ConnectionErrorView Visibility="{Binding IsConnected, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
