namespace CyberPrism.Tests.ViewModels
{
    public class ShellViewModelTests
    {
        /*
        [Fact]
        public void NavigateCommand_ShouldRequestNavigate()
        {
            // Arrange
            var regionManagerMock = new Mock<IRegionManager>();
            var settingsServiceMock = new Mock<IAppSettingsService>();
            settingsServiceMock.Setup(s => s.Settings).Returns(new AppSettings());

            var viewModel = new ShellViewModel(regionManagerMock.Object, settingsServiceMock.Object);
            string navigatePath = "DashboardView";

            // Act
            viewModel.NavigateCommand.Execute(navigatePath);

            // Assert
            regionManagerMock.Verify(rm => rm.RequestNavigate("ContentRegion", navigatePath), Times.Once);
        }
        */
    }
}

